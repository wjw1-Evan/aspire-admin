---
globs: Platform.AppHost/*.cs,Platform.ApiService/Program.cs,Platform.DataInitializer/Program.cs
description: .NET Aspire 微服务架构和编排规范
---
# .NET Aspire 微服务架构与编排规范

## 🎯 核心原则

- 统一通过 .NET Aspire 编排所有服务，集中处理依赖、服务发现、配置和监控
- MongoDB → DataInitializer → ApiService → 前端/移动端的依赖链必须完整，禁用绕过
- YARP 作为统一网关入口，禁止前端硬编码后端地址

## ✅ AppHost 关键配置要点

```csharp
// Platform.AppHost/AppHost.cs（核心骨架）
var builder = DistributedApplication.CreateBuilder(args);

var mongodb = builder.AddMongoDB("mongo")
    .WithMongoExpress()
    .WithLifetime(ContainerLifetime.Persistent)
    .AddDatabase("mongodb");

var datainitializer = builder.AddProject<Projects.Platform_DataInitializer>("datainitializer")
    .WithReference(mongodb)
    .WithHttpEndpoint()
    .WithHttpHealthCheck("/health");

var apiservice = builder.AddProject<Projects.Platform_ApiService>("apiservice")
    .WithReference(mongodb)
    .WithReference(datainitializer)
    .WithHttpHealthCheck("/health");

var yarp = builder.AddYarp("apigateway")
    .WithHostPort(15000)
    .WithConfiguration(config =>
        config.AddRoute("/{service}/{**catch-all}", config.AddCluster(apiservice))
              .WithTransformPathRouteValues("/{**catch-all}"));

builder.AddNpmApp("admin", "../Platform.Admin")
    .WithReference(yarp)
    .WaitFor(yarp)
    .WithHttpEndpoint(env: "PORT", port: 15001);

builder.AddNpmApp("app", "../Platform.App")
    .WithReference(yarp)
    .WaitFor(yarp)
    .WithHttpEndpoint(env: "PORT", port: 15002);

builder.Build().Run();
```

> 📌 提示：如需扩容，可对 `datainitializer` 保持单实例，对 `apiservice` 调整 `WithReplicas(n)`。

## ⚙️ 微服务 Program.cs 最小清单

### ApiService

```csharp
var builder = WebApplication.CreateBuilder(args);
builder.AddServiceDefaults();
builder.Services
    .AddMongoDb(builder.Configuration)
    .AddBusinessServices()
    .AddJwtAuthentication(builder.Configuration)
    .AddMiddlewareServices();

var app = builder.Build();
app.ConfigureMiddlewarePipeline();
app.Run();
```

### DataInitializer

```csharp
var builder = WebApplication.CreateBuilder(args);
builder.AddServiceDefaults();
builder.Services
    .AddMongoDb(builder.Configuration)
    .AddScoped<IDataInitializerService, DataInitializerService>();

var app = builder.Build();
app.ConfigureDataInitialization();
app.Run();
```

## 🌐 服务发现、配置与监控

- `AddServiceDiscovery()`：统一 HttpClient 配置，自动注入服务发现与韧性策略
- `AddConfigurationManagement()`：绑定 `JwtSettings`、`MongoDbSettings`、`AppSettings`，并根据环境加载 UserSecrets/Azure Key Vault
- `AddMonitoring()`：启用 OpenTelemetry（Tracing + Metrics），同时通过 `AddLogging()` 输出 Console/Debug/EventSource
- 健康检查：务必注册 `MongoDbHealthCheck`、`ApiServiceHealthCheck`、`DataInitializerHealthCheck`，并暴露 `/health`

## 🚀 部署与环境策略

- 开发、生产共用同一套骨架，差异仅在副本数及外部服务配置
- DataInitializer 始终单实例运行；ApiService 可按环境扩容
- 所有前端应用通过 YARP 访问后端，避免直连 ApiService

## 🚫 禁止事项

- **硬编码服务地址**：禁止在任何客户端设置固定 BaseAddress，必须依赖服务发现
- **跳过依赖链**：ApiService 必须引用 DataInitializer，保证索引/数据先完成
- **忽略健康检查**：所有服务都要暴露 `/health`，并在 AppHost 中声明 `WithHttpHealthCheck`

## ✅ 开发检查清单

- [ ] AppHost 中服务引用、依赖、健康检查齐全
- [ ] Program.cs 仅保留必要的服务注册，逻辑移至扩展方法
- [ ] 新增 HttpClient 均通过 `AddServiceDiscovery()` 获得地址
- [ ] 关键服务接入健康检查与 OpenTelemetry
- [ ] 不同环境的配置来源明确（Json、UserSecrets、KeyVault）
- [ ] 单元测试覆盖关键服务的启动与依赖

## 📚 参考资源

- [应用主机配置](mdc:Platform.AppHost/AppHost.cs)
- [API 服务配置](mdc:Platform.ApiService/Program.cs)
- [数据初始化服务配置](mdc:Platform.DataInitializer/Program.cs)
- [.NET Aspire 官方文档](https://learn.microsoft.com/en-us/dotnet/aspire/)
