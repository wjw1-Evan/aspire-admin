---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "datainitializer-deployment"
  labels:
    app.kubernetes.io/name: "platform-apphost"
    app.kubernetes.io/component: "datainitializer"
    app.kubernetes.io/instance: "{{.Release.Name}}"
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "platform-apphost"
        app.kubernetes.io/component: "datainitializer"
        app.kubernetes.io/instance: "{{.Release.Name}}"
    spec:
      containers:
        - image: "{{ .Values.parameters.datainitializer.datainitializer_image }}"
          name: "datainitializer"
          envFrom:
            - configMapRef:
                name: "datainitializer-config"
            - secretRef:
                name: "datainitializer-secrets"
          ports:
            - name: "http"
              protocol: "TCP"
              containerPort: "{{ .Values.parameters.datainitializer.port_http }}"
          imagePullPolicy: "IfNotPresent"
  selector:
    matchLabels:
      app.kubernetes.io/name: "platform-apphost"
      app.kubernetes.io/component: "datainitializer"
      app.kubernetes.io/instance: "{{.Release.Name}}"
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
