---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "datainitializer-service"
  labels:
    app.kubernetes.io/name: "platform-apphost"
    app.kubernetes.io/component: "datainitializer"
    app.kubernetes.io/instance: "{{.Release.Name}}"
spec:
  type: "ClusterIP"
  selector:
    app.kubernetes.io/name: "platform-apphost"
    app.kubernetes.io/component: "datainitializer"
    app.kubernetes.io/instance: "{{.Release.Name}}"
  ports:
    - name: "http"
      protocol: "TCP"
      port: "{{ .Values.parameters.datainitializer.port_http }}"
      targetPort: "{{ .Values.parameters.datainitializer.port_http }}"
