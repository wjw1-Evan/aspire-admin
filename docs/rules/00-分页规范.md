# 分页处理统一规范

> 本文档是 Aspire Admin 项目的分页规范核心文档，统一定义前后端的分页类型、参数命名、接口设计等。所有后端和前端开发必须遵循本规范。

---

## 1. 统一分页类型

### 1.1 PagedResult<T> 结构

前后端统一使用以下分页响应结构：

| 属性 | 类型 | 说明 |
|------|------|------|
| `queryable` | `T[]` | 分页后的数据数组 |
| `currentPage` | `number` | 当前页码 |
| `pageSize` | `number` | 每页数量 |
| `rowCount` | `number` | 总记录数 |
| `pageCount` | `number` | 总页数 |

### 1.2 后端类型定义

```csharp
using System.Linq.Dynamic.Core;

public class PagedResult<T>
{
    public IQueryable<T> Queryable { get; set; } = Enumerable.Empty<T>().AsQueryable();
    public int CurrentPage { get; set; }
    public int PageSize { get; set; }
    public int RowCount { get; set; }
    public int PageCount { get; set; }
}
```

> 位置：`Platform.ServiceDefaults/Models/PagedResult.cs`

### 1.3 前端类型定义

```typescript
// src/types/unified-api.ts

export interface PagedResult<T> {
  queryable: T[];
  currentPage: number;
  pageSize: number;
  rowCount: number;
  pageCount: number;
}
```

---

## 2. 统一分页参数

### 2.1 参数命名规范

| 参数 | 说明 | 禁止使用 |
|------|------|----------|
| `page` | 当前页码（从 1 开始） | `current`、`pageIndex`、`pageNumber`、`currentPage` |
| `pageSize` | 每页数量 | `limit`、`size`、`perPage` |

### 2.2 后端请求模型

```csharp
public class PageParams
{
    public int Page { get; set; } = 1;
    public int PageSize { get; set; } = 10;
}

public class FileListQuery : PageParams
{
    public string? Keyword { get; set; }
    public string? SortBy { get; set; }
    public string? SortOrder { get; set; }
}
```

### 2.3 前端请求参数

```typescript
interface FileListParams {
  page: number;
  pageSize: number;
  keyword?: string;
  sortBy?: string;
  sortOrder?: 'asc' | 'desc';
}
```

---

## 3. 统计与分页接口分离

### 3.1 设计原则

统计数据和分页数据必须**分离为两个独立接口**：

| 接口类型 | 用途 | 数据范围 |
|----------|------|----------|
| 统计接口 | 计算全局统计 | 基于全部数据 |
| 分页接口 | 表格展示 | 只返回当前页 |

### 3.2 原因

1. 分页数据只包含当前页的记录，无法用于计算全局统计
2. 前端不应为计算统计而加载大量数据（pageSize: 1000）
3. 统计计算应在后端完成，前端直接使用

### 3.3 后端实现

```csharp
// 控制器
[HttpGet]
public async Task<IActionResult> GetRoles(
    [FromQuery] int page = 1,
    [FromQuery] int pageSize = 20)
{
    var result = await _roleService.GetRolesPaginatedAsync(page, pageSize);
    return Success(result);
}

[HttpGet("statistics")]
public async Task<IActionResult> GetRoleStatistics()
{
    var statistics = await _roleService.GetRoleStatisticsAsync();
    return Success(statistics);
}
```

### 3.4 前端实现

```typescript
// 服务层
import type { ApiResponse, PagedResult } from '@/types/unified-api';
import type { Role, RoleStatistics } from './types';

export async function getRoles(params: { page: number; pageSize: number }) {
  return request<ApiResponse<PagedResult<Role>>>('/api/roles', { params });
}

export async function getRoleStatistics() {
  return request<ApiResponse<RoleStatistics>>('/api/roles/statistics');
}

// 页面组件
const loadStatistics = async () => {
  const response = await getRoleStatistics();
  if (response.success && response.data) {
    setStatistics(response.data);
  }
};

const loadRoleData = async (params: { page: number; pageSize: number }) => {
  const response = await getRoles(params);
  if (response.success && response.data) {
    return {
      data: response.data.queryable,
      total: response.data.rowCount,
      success: true,
    };
  }
};

// 初始化时分别加载
useEffect(() => {
  loadStatistics();
}, []);
```

### 3.5 ❌ 错误做法

```typescript
// ❌ 错误：为计算统计加载大量数据
const response = await getRoles({ page: 1, pageSize: 1000 });
const data = response.data.queryable;
const stats = {
  total: data.length,
  active: data.filter(r => r.isActive).length,  // 只统计了第一页！
};
```

---

## 4. 后端实现规范

### 4.1 服务层分页查询

```csharp
public async Task<PagedResult<FileItem>> GetFileItemsAsync(FileListQuery query)
{
    var q = _context.Set<FileItem>().Where(x => x.Status == FileStatus.Active);

    // 动态排序（必须先排序）
    q = query.SortBy?.ToLower() switch
    {
        "name" => query.SortOrder == "desc" ? q.OrderByDescending(f => f.Name) : q.OrderBy(f => f.Name),
        "size" => query.SortOrder == "desc" ? q.OrderByDescending(f => f.Size) : q.OrderBy(f => f.Size),
        _ => q.OrderByDescending(f => f.CreatedAt)
    };

    return q.PageResult(query.Page, query.PageSize);
}
```

### 4.2 必须先排序

使用 `PageResult` 前，查询**必须先排序**：

```csharp
// ✅ 正确：先排序再分页
var result = query.OrderByDescending(x => x.CreatedAt).PageResult(page, pageSize);

// ❌ 错误：未排序直接分页会抛异常
var result = query.PageResult(page, pageSize);
```

### 4.3 控制器层

控制器直接调用服务层方法，**不在控制器层处理分页逻辑**：

```csharp
[HttpGet("versions")]
public async Task<IActionResult> GetVersionHistory(
    [FromQuery] string fileId,
    [FromQuery] int page = 1,
    [FromQuery] int pageSize = 50)
{
    var pagedResult = await _fileVersionService.GetVersionHistoryPaginatedAsync(fileId, page, pageSize);
    return Success(pagedResult);
}
```

---

## 5. 前端实现规范

### 5.1 服务层定义

```typescript
import { request } from '@umijs/max';
import type { ApiResponse, PagedResult } from '@/types/unified-api';
import type { FileItem } from './types';

export async function getFileList(params: { page: number; pageSize: number; keyword?: string }) {
  return request<ApiResponse<PagedResult<FileItem>>>('/api/files', { params });
}
```

### 5.2 页面组件使用

```typescript
const fetchFileList = async (params: { page: number; pageSize: number }) => {
  const response = await getFileList(params);

  if (response.success && response.data) {
    return {
      data: response.data.queryable,
      total: response.data.rowCount,
      success: true,
    };
  }

  return { data: [], total: 0, success: false };
};
```

### 5.3 访问分页数据

```typescript
const response = await getFileList({ page: 1, pageSize: 20 });
if (response.success && response.data) {
  const { queryable, rowCount, pageCount, currentPage, pageSize } = response.data;
  // queryable: 数据数组
  // rowCount: 总记录数
  // pageCount: 总页数
  // currentPage: 当前页码
  // pageSize: 每页数量
}
```

---

## 6. 常见错误示例

### 6.1 后端错误

```csharp
// ❌ 错误 1：手动分页（绕过 DynamicLinq）
var skip = (page - 1) * pageSize;
var items = await q.Skip(skip).Take(pageSize).ToListAsync();

// ❌ 错误 2：先 ToListAsync 再分页（内存分页）
var all = await q.ToListAsync();
return all.Skip((page - 1) * pageSize).Take(pageSize).ToList();

// ❌ 错误 3：控制器中处理分页
var versions = await _service.GetVersionHistoryAsync(fileId);
var pagedResult = versions.AsQueryable().PageResult(page, pageSize);

// ❌ 错误 4：未排序直接分页
var result = query.PageResult(page, pageSize);

// ❌ 错误 5：使用非标准参数名
public async Task<IActionResult> GetItems([FromQuery] int current, [FromQuery] int pageSize)
```

### 6.2 前端错误

```typescript
// ❌ 错误 1：使用非标准参数名
const response = await getItems({ current: 1, pageSize: 20 });

// ❌ 错误 2：未检查 success 字段
const data = response.data.queryable;

// ❌ 错误 3：重复定义已有类型
interface PagedResult<T> {
  data: T[];
  total: number;
  // 自定义的，与规范不一致
}

// ❌ 错误 4：为计算统计加载大量数据
const response = await getRoles({ page: 1, pageSize: 1000 });
const stats = { total: response.data.queryable.length };
```

---

## 7. 相关代码位置

| 组件 | 位置 |
|------|------|
| 后端分页类型 | `Platform.ServiceDefaults/Models/PagedResult.cs` |
| 后端分页服务参考 | `Platform.ApiService/Services/CloudStorageService.cs` |
| 前端统一类型 | `Platform.Admin/src/types/unified-api.ts` |
| 前端服务层参考 | `Platform.Admin/src/services/cloud-storage/api.ts` |

---

## 8. 相关文档

| 文档 | 说明 |
|------|------|
| [`backend/04-分页处理规范.md`](./backend/04-分页处理规范.md) | 后端分页实现详细说明 |
| [`frontend-admin/04-TypeScript类型安全.md`](./frontend-admin/04-TypeScript类型安全.md) | 前端类型安全规范 |
