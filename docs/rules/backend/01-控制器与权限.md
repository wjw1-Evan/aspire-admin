# 控制器与权限规范

本规范结合 AGENTS.md 最高准则，统一控制器分层、权限注解、响应标准。

## 1. 控制器分层与继承

- **[强制]** 所有业务控制器必须继承 `BaseApiController`，禁止直接继承 `ControllerBase` 或返回裸 JSON。
- 控制器层**只负责**路由、参数校验、权限注解 `[RequireMenu]`、调用服务层、返回统一响应。
- **禁止**在控制器注入/操作 `DbContext`、`IMongoCollection`、`IMongoDatabase`，所有数据访问必须下沉到服务层。
- 复杂业务、批量处理、关联查询等逻辑必须全部下沉到服务层封装。

```csharp
// ✅ 正确：仅调用服务层，返回统一响应
public class TaskController : BaseApiController
{
    [HttpGet("{taskId}")]
    [RequireMenu("task-view")]
    public async Task<IActionResult> GetTask(string taskId)
    {
        var task = await _taskService.GetTaskByIdAsync(taskId);
        if (task == null)
            return NotFoundError("任务", taskId);
        return Success(task);
    }
}

// ❌ 错误：直接继承 ControllerBase 或返回裸类型
public class WrongController : ControllerBase
{
    [HttpGet]
    public async Task<User> GetUser()
    {
        return await _userService.GetUserByIdAsync(id);
    }
}
```

## 2. 权限注解唯一标准

- **[强制]** 所有敏感操作必须添加 `[RequireMenu("menu-action")]` 注解，禁止使用 `HasPermission()`、`RequirePermission()` 等旧方法。
- 支持多个菜单名称，用户拥有任意一个即可访问。
- 菜单名称统一用连字符 `-` 分隔，格式为 `模块-资源`。

```csharp
// ✅ 正确：使用 RequireMenu（单个菜单）
[HttpPost("create")]
[RequireMenu("task-management")]
public async Task<IActionResult> CreateTask([FromBody] CreateTaskRequest request) { }

// ✅ 正确：使用 RequireMenu（多个菜单，满足其一即可）
[HttpPost("list")]
[RequireMenu("workflow-list", "workflow-monitor")]
public async Task<IActionResult> GetWorkflows() { }

[HttpPut("{id}/activate")]
[RequireMenu("user-management")]  // 用户管理类操作
public async Task<IActionResult> ActivateUser(string id) { }

// ❌ 禁止：使用废弃方法
[RequirePermission("users.create")]
public async Task<IActionResult> CreateUser([FromBody] CreateUserRequest request) { }
```

### 用户上下文获取

#### 获取当前用户 ID

使用 `BaseApiController` 提供的属性和方法获取当前用户信息：

```csharp
public class PasswordBookController : BaseApiController
{
    // ✅ 正确：使用基类提供的方法
    protected string? CurrentUserId => GetCurrentUserId();        // 可空
    protected string GetRequiredUserId();                          // 抛异常

    // ✅ 正确：获取企业 ID
    protected Task<string?> GetCurrentCompanyIdAsync();
    protected Task<string> GetRequiredCompanyIdAsync();

    // ✅ 正确：检查登录状态
    protected bool IsAuthenticated => !string.IsNullOrEmpty(CurrentUserId);
}
```

#### 获取企业上下文

通过 `ITenantContext` 获取企业信息：

```csharp
// ✅ 正确：通过 ITenantContext 获取
var companyId = await GetCurrentCompanyIdAsync();
var isAdmin = await _tenantContext.IsAdminAsync();

// ❌ 禁止：直读 JWT Claims
var companyId = User.FindFirst("companyId")?.Value;
```

#### 用户 ID 传递给服务

控制器获取用户 ID 后，应传递给服务层处理：

```csharp
// ✅ 正确：控制器获取 userId 并传递给服务
public async Task<IActionResult> CreateEntry([FromBody] CreateRequest request)
{
    var userId = GetRequiredUserId();  // 控制器获取
    var result = await _service.CreateEntryAsync(request, userId);  // 传递给服务
    return Success(result);
}

// ❌ 禁止：在服务中直接使用 ITenantContext 获取 userId
public async Task CreateEntryAsync(CreateRequest request)
{
    var userId = _tenantContext.GetCurrentUserId();  // 不推荐
}
```

---

## 菜单定义规范

### 菜单名称（Menu.Name 字段）

- 统一使用连字符 `-` 分隔，格式为 `模块-资源`。
- 示例：`workflow-list`、`workflow-monitor`、`project-management-task`
- **注意**：菜单名称与 RequireMenu 属性中使用的名称一致。

### 菜单定义位置

- 所有系统菜单在 `Platform.DataInitializer/Menus.json` 中定义。

### 子菜单映射

- 新增子菜单时，在 `ParentId` 字段指定父菜单 ID。

### 前端翻译

- 菜单翻译键格式为 `menu.模块.资源`（使用 `.` 分隔）。
- 在 `src/locales/*/menu.ts` 中维护。

---

## 常见错误示例

```csharp
// ❌ 错误 1：缺少 RequireMenu
[HttpPut("{id}/deactivate")]
public async Task<IActionResult> DeactivateUser(string id)
{
    await _userService.DeactivateUserAsync(id);
    return Success("用户已禁用");
}

// ✅ 正确：添加 RequireMenu
[HttpPut("{id}/deactivate")]
[RequireMenu("user-management")]
public async Task<IActionResult> DeactivateUser(string id)
{
    await _userService.DeactivateUserAsync(id);
    return Success("用户已禁用");
}

// ❌ 错误 2：直读 JWT
[HttpGet("list")]
public async Task<IActionResult> GetUsers()
{
    var companyId = User.Claims.FirstOrDefault(c => c.Type == "companyId")?.Value;
    var users = await _userService.GetUsersAsync(companyId);
    return Success(users);
}

// ✅ 正确：通过 ITenantContext
[HttpGet("list")]
[RequireMenu("user-management")]
public async Task<IActionResult> GetUsers()
{
    var companyId = await _tenantContext.GetCurrentCompanyIdAsync();
    var users = await _userService.GetUsersAsync(companyId);
    return Success(users);
}
```

### 请求 DTO 位置规范

- **[强制]** 不要在 Controller 文件内定义请求 DTO（如 `CreateXRequest/UpdateXRequest`）。
- **[建议]** 请求 DTO 放到 `Platform.ApiService/Models/<领域>/` 下，或按模块拆分成独立 `*Requests.cs` 文件（例如本次把 `WorkflowController` 的请求 DTO 下沉到 `Models/Workflow/WorkflowControllerRequests.cs`）。

### 接口/契约尽量使用独立文件

- **[强制]** Service 接口（如 `IXXXService`、`IXXXQueryService`）与实现（`XXXService`）应拆分到各自独立文件：
  - 接口：`Platform.ApiService/Services/I*.cs`
  - 实现：`Platform.ApiService/Services/*Service.cs`
- **[强制]** 如果 Controller 需要复用的请求/响应契约较多，优先放到 `Models/<领域>/` 的独立 `*Requests.cs` / `*Responses.cs` 文件，避免在单个 Controller 里堆叠。
