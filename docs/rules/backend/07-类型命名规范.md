# 类型命名规范

本规范结合 AGENTS.md 最高准则，统一类型命名与命名空间导入方式。

## 0. 服务命名规范

### 0.1 禁止使用 "Dependency" 单词

**[强制]** 服务接口和实现类名称中禁止使用 `Dependency` 单词。

**原因**：服务自动注册逻辑会过滤掉包含 "Dependency" 的接口名，导致依赖注入失败。

```csharp
// ❌ 错误：名称包含 Dependency
public interface ITaskDependencyService { }
public class TaskDependencyService : ITaskDependencyService { }

// ✅ 正确：使用其他同义词
public interface ITaskRelationService { }
public class TaskRelationService : ITaskRelationService { }
```

**允许的同义词**：
| 概念 | 禁用词 | 替换词 |
|------|--------|--------|
| 任务依赖关系 | TaskDependency | TaskRelation, TaskLink, TaskPredecessor |
| 工作流依赖 | WorkflowDependency | WorkflowRelation, FlowLink |
| 实体关联 | EntityDependency | EntityRelation, EntityLink |

### 0.2 服务生命周期命名约定

服务生命周期由类名前缀决定：

| 前缀 | 生命周期 | 说明 |
|------|----------|------|
| `Singleton*` | Singleton | 应用级唯一实例 |
| 无前缀 | Scoped | 请求级唯一实例（默认） |
| `Transient*` | Transient | 每次获取都是新实例 |

```csharp
// ✅ Singleton 示例
public class SingletonPasswordEncryptionService : IPasswordEncryptionService { }
public class SingletonWorkflowEngine : IWorkflowEngine { }

// ✅ 默认 Scoped 示例（无需前缀）
public class UserService : IUserService { }
public class TaskCrudService : ITaskCrudService { }

// ❌ 错误：禁止使用 Dependency 相关后缀
public class TaskDependencyService : ITaskDependencyService { }  // 会被过滤！
```

### 0.3 服务文件组织约定

服务必须遵循以下文件命名和组织方式：

| 类型 | 文件名 | 类/接口命名 |
|------|--------|------------|
| 接口 | `I{ServiceName}Service.cs` | `I{ServiceName}Service` |
| 实现 | `{ServiceName}Service.cs` | `{ServiceName}Service` |

```csharp
// ✅ 正确：一个文件只包含接口
// IUserService.cs
public interface IUserService { }

// UserService.cs
public class UserService : IUserService { }

// ❌ 错误：文件包含多个类型
// IUserService.cs
public interface IUserService { }
public interface IUserQueryService { }  // 不应在此文件
```

---

## 4. 禁止 using 别名

- **[强制]** 禁止使用 `using Xxx = Yyy.Zzz` 别名写法，必须直接使用完整类型名。
- 原因：类型来源不明确、可读性降低、IDE 提示不准确、重构困难。

```csharp
// ❌ 错误：类型别名
using TaskModel = Platform.ApiService.Models.WorkTask;
using TaskStatusEnum = Platform.ApiService.Models.TaskStatus;
using User = Platform.ApiService.Models.AppUser;
public class TaskService
{
    private async Task<List<TaskModel>> GetTasksAsync()
    {
        if (t.Status == TaskStatusEnum.Pending) { }
    }
}
```

## 5. 正确命名与导入方式

直接使用完整类型名或统一导入命名空间：

```csharp
using Platform.ApiService.Models;
public class TaskService : ITaskService
{
    private readonly DbContext _context;
    public async Task<List<WorkTask>> GetTasksAsync()
    {
        var tasks = await _context.Set<WorkTask>()
            .Where(t => t.Status == Models.TaskStatus.Pending)
            .ToListAsync();
        return tasks;
    }
}
```

## 6. 变更与维护建议

- 命名如需扩展，优先通过命名空间导入，禁止自定义别名。
- 发现实现与本规范或 AGENTS.md 不符时，优先以本规范为准，及时修订文档与代码。
            if (task.Status == TaskStatus.Pending)  // 直接使用枚举
            {
                // 处理逻辑
            }
        }
    }
}

```

---

## 常见类型使用规范

### 实体类型
| 类型 | 命名空间 | 正确用法 |
|------|----------|----------|
| `WorkTask` | `Platform.ApiService.Models` | `private readonly DbContext _context` |
| `User` / `AppUser` | `Platform.ApiService.Models` | `var userMap = new Dictionary<string, AppUser>()` |
| `Project` | `Platform.ApiService.Models` | `private readonly DbContext _context` |

### 枚举类型
```csharp
// ✅ 正确：使用完整命名空间
if (task.Status == Models.TaskStatus.Pending) { }
if (task.Priority == Models.TaskPriority.High) { }

// ✅ 正确：导入命名空间后直接使用
using Platform.ApiService.Models;
if (task.Status == TaskStatus.Pending) { }
```

### DTO 类型

```csharp
// ✅ 正确：使用 Models 命名空间中的 DTO
using Platform.ApiService.Models;

public TaskDto ConvertToDto(WorkTask task)
{
    return new TaskDto
    {
        Id = task.Id,
        TaskName = task.TaskName,
        Status = (int)task.Status
    };
}
```

---

## 常见错误示例

### ❌ 错误 1：类型别名混淆

```csharp
using TaskModel = Platform.ApiService.Models.WorkTask;
using AnotherTask = Platform.Other.Models.WorkTask;

public void Process()
{
    var t1 = new TaskModel();   // 哪个命名空间？
    var t2 = new AnotherTask(); // 需要查别名定义
}
```

### ❌ 错误 2：循环别名导致

```csharp
// 在多个文件中定义相同的别名
// File1.cs
using User = Platform.ApiService.Models.AppUser;

// File2.cs
using User = Platform.ApiService.Models.UserEntity;  // 冲突！

// 导致混淆和 bug
```

### ✅ 正确：明确命名空间

```csharp
// 直接使用完整类型
var userMap = new Dictionary<string, Platform.ApiService.Models.AppUser>();

// 或导入命名空间
using AppUser = Platform.ApiService.Models.AppUser;
var userMap = new Dictionary<string, AppUser>();
```

---

## 迁移指南

### 查找使用别名的文件

```bash
# 查找所有 using 别名
grep -r "using.*=" Platform.ApiService/Services/ --include="*.cs"
```

### 迁移步骤

1. 找到文件中所有的类型别名
2. 移除 `using Xxx = Yyy.Zzz;` 行
3. 替换所有类型别名引用为完整类型名或命名空间前缀
4. 确保命名空间已导入
5. 编译验证

### 示例迁移

```csharp
// 迁移前（错误使用别名）
using TaskModel = Platform.ApiService.Models.WorkTask;
using TaskStatusEnum = Platform.ApiService.Models.TaskStatus;

public async Task<List<TaskModel>> GetTasksAsync()
{
    var tasks = await _taskFactory.FindAsync(t =>
        t.Status == TaskStatusEnum.Pending);
}

// 迁移后（正确使用 DbContext + 命名空间导入）
using Platform.ApiService.Models;

public async Task<List<WorkTask>> GetTasksAsync()
{
    var tasks = await _context.Set<WorkTask>()
        .Where(t => t.Status == TaskStatus.Pending)
        .ToListAsync();
}
```

---

## IDE 配置建议

### ReSharper / Rider

- 启用 "Use fully qualified type names" 规则
- 禁用 "Use shortened 'using' directives"

### Visual Studio

- 安装 "Treat Warnings as Errors" 插件
- 自定义规则检查别名使用

### EditorConfig

```ini
# .editorconfig
dotnet_styleQualification_for_field = true
dotnet_styleQualification_for_property = true
dotnet_styleQualification_for_type_parameter = true
```
