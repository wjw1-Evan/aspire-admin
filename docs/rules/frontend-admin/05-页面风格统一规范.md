# 页面风格统一规范

本规范补充接口交互相关内容，强调所有页面风格、数据流、接口调用、分页、权限、错误处理等与后端标准一致。

> **[强制]** 所有列表/管理页面必须遵循以下统一风格，并与后端 API 结构、权限、分页、错误处理等保持一致。

## 1. 页面容器

统一使用 `@ant-design/pro-components` 的 `PageContainer`，样式如下：

```tsx
import { PageContainer } from '@ant-design/pro-components';

<PageContainer>
  {/* 页面内容 */}
</PageContainer>
```

- 统一使用 `@ant-design/pro-components` 的 PageContainer
- **禁止**自定义 padding 或 margin 覆盖默认样式

## 2. 统计卡片区域

### 推荐方案：使用 ProTable headerTitle + Tag

优先使用 `ProTable` 的 `headerTitle` 属性，在表格标题行内嵌入统计数据，使用 `Tag` 组件展示：

```tsx
import { Tag, Space } from 'antd';
import { LockOutlined } from '@ant-design/icons';

<ProTable
  actionRef={actionRef}
  headerTitle={
    <Space size={24}>
      <Space><LockOutlined />密码本</Space>
      <Space size={12}>
        <Tag color="blue">总条目 {statistics?.totalEntries || 0}</Tag>
        <Tag color="green">分类 {statistics?.categoryCount || 0}</Tag>
        <Tag color="orange">标签 {statistics?.tagCount || 0}</Tag>
        <Tag color="purple">最近使用 {statistics?.recentUsedCount || 0}</Tag>
      </Space>
    </Space>
  }
  // ... 其他配置
/>
```

**优点**：
- 统计信息与表格标题在同一行，紧凑美观
- 使用 `Tag` 组件，视觉层次清晰
- 响应式布局，`Space` 自动处理间距

### 备选方案：使用 ProCard gutter 布局

当统计数据较多或需要更详细的展示时，可使用 `ProCard` 的 `gutter` 属性：

```tsx
import { ProCard } from '@ant-design/pro-components';

<ProCard gutter={16} style={{ marginBottom: 16 }}>
  <ProCard colSpan={{ xs: 24, sm: 12, md: 6 }}>
    <div style={{ fontSize: 24, fontWeight: 'bold', color: '#1890ff' }}>100</div>
    <div style={{ color: '#8c8c8c', fontSize: 12 }}>总用户数</div>
  </ProCard>
  <ProCard colSpan={{ xs: 24, sm: 12, md: 6 }}>
    <div style={{ fontSize: 24, fontWeight: 'bold', color: '#52c41a' }}>80</div>
    <div style={{ color: '#8c8c8c', fontSize: 12 }}>活跃用户</div>
  </ProCard>
  <ProCard colSpan={{ xs: 24, sm: 12, md: 6 }}>
    <div style={{ fontSize: 24, fontWeight: 'bold', color: '#faad14' }}>10</div>
    <div style={{ color: '#8c8c8c', fontSize: 12 }}>管理员</div>
  </ProCard>
  <ProCard colSpan={{ xs: 24, sm: 12, md: 6 }}>
    <div style={{ fontSize: 24, fontWeight: 'bold', color: '#722ed1' }}>5</div>
    <div style={{ color: '#8c8c8c', fontSize: 12 }}>新增用户</div>
  </ProCard>
</ProCard>
```

### 备选方案：StatCard 组件

如需使用 StatCard 组件，配合 Card + Row/Col：

```tsx
import { StatCard } from '@/components';
import { Card, Row, Col } from 'antd';

<Card style={{ marginBottom: 16 }}>
  <Row gutter={[12, 12]}>
    <Col xs={24} sm={12} md={6}>
      <StatCard title="总任务数" value={statistics.totalTasks} icon={<TaskIcon />} color="#1890ff" />
    </Col>
  </Row>
</Card>
```

## 3. 数据表格 (ProTable)

### 基本配置

所有列表页面统一使用 `ProTable` 组件：

```tsx
import { ProTable } from '@ant-design/pro-table';
import { Input } from 'antd';
import { SearchOutlined, ReloadOutlined, PlusOutlined } from '@ant-design/icons';

<ProTable
  actionRef={actionRef}
  request={async (params) => {
    const { current, pageSize } = params;
    const res = await api.list({ page: current, pageSize, search: state.search });
    return { data: res.data?.queryable || [], total: res.data?.rowCount || 0, success: res.success };
  }}
  columns={columns}
  rowKey="id"
  search={false}
  scroll={{ x: 'max-content' }}
  toolBarRender={() => [
    <Input.Search
      key="search"
      placeholder="搜索..."
      allowClear
      value={state.search}
      onChange={(e) => set({ search: e.target.value })}
      onSearch={(value) => { set({ search: value }); actionRef.current?.reload(); }}
      style={{ width: 260, marginRight: 8 }}
      prefix={<SearchOutlined />}
    />,
    <Button key="refresh" icon={<ReloadOutlined />} onClick={handleRefresh}>刷新</Button>,
    <Button key="create" type="primary" icon={<PlusOutlined />} onClick={() => handleOpenModal(null)}>
      新建
    </Button>,
  ]}
/>
```

### 关键配置说明

| 属性 | 值 | 说明 |
|------|-----|------|
| `search={false}` | false | 禁用 ProTable 内置搜索表单，使用自定义搜索框 |
| `scroll={{ x: 'max-content' }}` | x: 'max-content' | **必须添加**，让列宽自动适应内容 |
| `toolBarRender` | function | **必须包含**搜索框、刷新、新建等操作按钮 |

### 搜索框统一规范

搜索框必须：
1. 使用 `Input.Search` 组件
2. 添加 `prefix={<SearchOutlined />}` 图标
3. 使用受控模式 `value` + `onChange`
4. 搜索时触发 `actionRef.current?.reload()`
5. 宽度固定 `260px`，右边距 `8px`

```tsx
<Input.Search
  placeholder="搜索..."
  allowClear
  value={state.search}
  onChange={(e) => set({ search: e.target.value })}
  onSearch={(value) => { set({ search: value }); actionRef.current?.reload(); }}
  style={{ width: 260, marginRight: 8 }}
  prefix={<SearchOutlined />}
/>
```

### 列配置规范

```tsx
const columns: ProColumns<DataType>[] = [
  {
    title: '名称',
    dataIndex: 'name',
    ellipsis: true,
    sorter: true,
  },
  {
    title: '状态',
    dataIndex: 'status',
    render: (_, record) => <Tag color="blue">{record.statusName}</Tag>,
  },
  // 操作列固定在右侧
  {
    title: '操作',
    key: 'action',
    width: 150,
    fixed: 'right',
    valueType: 'option',
    render: (_, record) => (
      <Space>
        <Button type="link" size="small" onClick={() => handleEdit(record)}>编辑</Button>
        <Button type="link" size="small" danger onClick={() => handleDelete(record)}>删除</Button>
      </Space>
    ),
  },
];
```

### 操作列规范

**宽度计算规则**：

操作列宽度应根据按钮数量动态计算：

| 按钮数量 | 推荐宽度 | 计算依据 |
|---------|---------|---------|
| 2个按钮 | 150px | 每个按钮约60px + 间距 |
| 3个按钮 | 200-240px | 每个按钮约60px + 间距 |
| 4个按钮 | 280-320px | 每个按钮约60px + 间距 |

> 每个按钮（含图标+文字+padding）约需 60-80px，按钮间距使用 `Space size={4}`

**标准样式（参考密码本页面）**：

所有列表页面的操作列必须使用以下统一样式：

```tsx
{
  title: '操作',
  key: 'action',
  valueType: 'option',
  fixed: 'right',
  width: 180, // 2个按钮; 3个按钮用 200-240px
  render: (_, record) => (
    <Space size={4}>
      <Button variant="link" color="cyan" size="small" icon={<EyeOutlined />} onClick={() => handleView(record.id)}>
        查看
      </Button>
      <Button type="link" size="small" icon={<EditOutlined />} onClick={() => handleEdit(record)}>
        编辑
      </Button>
      <Popconfirm title={`确定删除「${record.name}」？`} onConfirm={() => handleDelete(record.id)}>
        <Button type="link" size="small" danger icon={<DeleteOutlined />}>
          删除
        </Button>
      </Popconfirm>
    </Space>
  ),
}
```

**关键要点**：
- `width`：根据按钮数量计算（2个→150px，3个→200-240px，4个→280-320px）
- `Space size={4}`：紧凑间距
- 查看按钮：`variant="link" color="cyan"`，强调色
- 编辑按钮：`type="link"`，次要操作
- 删除按钮：`type="link" danger`，使用 Popconfirm 包裹
- 所有按钮均设置 `size="small"` 和图标
- **删除确认信息**：格式为 `确定删除「{record.xxx}」？`，取记录中具有标识性的字段

## 4. 详情展示 (ProDescriptions)

使用 `ProDescriptions` 的子组件方式，**不要**使用 `items` 属性：

```tsx
import { ProDescriptions } from '@ant-design/pro-components';

<ProDescriptions column={2} bordered size="small">
  <ProDescriptions.Item label="名称">{data.name}</ProDescriptions.Item>
  <ProDescriptions.Item label="状态">
    <Tag color="blue">{data.status}</Tag>
  </ProDescriptions.Item>
  <ProDescriptions.Item label="描述" span={2}>
    {data.description || '-'}
  </ProDescriptions.Item>
  <ProDescriptions.Item label="创建时间">
    {dayjs(data.createdAt).format('YYYY-MM-DD HH:mm:ss')}
  </ProDescriptions.Item>
</ProDescriptions>
```

## 5. 响应式布局规范

### 5.1 断点定义

使用 Ant Design Grid 响应式断点：

| 断点 | 屏幕宽度 | 适用场景 | 列数 |
|------|---------|----------|------|
| `xs` | < 576px | 手机竖屏 | 单列 (colSpan=24) |
| `sm` | ≥ 576px | 手机横屏/小平板 | 双列 (colSpan=12) |
| `md` | ≥ 768px | 平板 | 三列 (colSpan=8) |
| `lg` | ≥ 992px | 小桌面 | 四列 (colSpan=6) |
| `xl` | ≥ 1200px | 桌面 | 六列 (colSpan=4) |
| `xxl` | ≥ 1600px | 大桌面 | 八列 (colSpan=3) |

### 5.2 Grid 布局规范

所有响应式布局统一使用 Ant Design 的 `Row` + `Col` 组合：

```tsx
import { Row, Col, Card } from 'antd';

<Row gutter={[16, 16]}>
  <Col xs={24} sm={12} md={8} lg={6}>
    <Card>内容块</Card>
  </Col>
  <Col xs={24} sm={12} md={8} lg={6}>
    <Card>内容块</Card>
  </Col>
  <Col xs={24} sm={12} md={8} lg={6}>
    <Card>内容块</Card>
  </Col>
  <Col xs={24} sm={12} md={8} lg={6}>
    <Card>内容块</Card>
  </Col>
</Row>
```

### 5.3 ProCard 响应式布局

使用 `ProCard` 的 `colSpan` 属性：

```tsx
import { ProCard } from '@ant-design/pro-components';

<ProCard gutter={16}>
  <ProCard colSpan={{ xs: 24, sm: 12, md: 8, lg: 6 }}>内容</ProCard>
  <ProCard colSpan={{ xs: 24, sm: 12, md: 8, lg: 6 }}>内容</ProCard>
  <ProCard colSpan={{ xs: 24, sm: 12, md: 8, lg: 6 }}>内容</ProCard>
  <ProCard colSpan={{ xs: 24, sm: 12, md: 8, lg: 6 }}>内容</ProCard>
</ProCard>
```

### 5.4 统计卡片响应式布局

统计卡片推荐使用 ProCard 配合响应式 colSpan：

```tsx
<ProCard gutter={16} style={{ marginBottom: 16 }}>
  <ProCard colSpan={{ xs: 24, sm: 12, md: 8, lg: 6 }} loading={loading}>
    <Statistic title="总用户数" value={statistics.totalUsers} />
  </ProCard>
  <ProCard colSpan={{ xs: 24, sm: 12, md: 8, lg: 6 }} loading={loading}>
    <Statistic title="活跃用户" value={statistics.activeUsers} />
  </ProCard>
  <ProCard colSpan={{ xs: 24, sm: 12, md: 8, lg: 6 }} loading={loading}>
    <Statistic title="新增用户" value={statistics.newUsers} />
  </ProCard>
  <ProCard colSpan={{ xs: 24, sm: 12, md: 8, lg: 6 }} loading={loading}>
    <Statistic title="管理员" value={statistics.adminCount} />
  </ProCard>
</ProCard>
```

### 5.5 详情页响应式布局

使用 `ProDescriptions` 的 `column` 属性结合 Responsive：

```tsx
import { useResponsive } from '@ant-design/pro-components';

const { screen } = useResponsive();

<ProDescriptions
  column={screen.lg ? 3 : screen.md ? 2 : 1}
  bordered
  size="small"
>
  <ProDescriptions.Item label="名称">{data.name}</ProDescriptions.Item>
  <ProDescriptions.Item label="状态">{data.status}</ProDescriptions.Item>
  <ProDescriptions.Item label="描述" span={2}>{data.description}</ProDescriptions.Item>
</ProDescriptions>
```

### 5.6 抽屉/表单响应式宽度

抽屉和表单宽度应随屏幕响应式调整：

```tsx
<Drawer
  width={screen.xs ? '100%' : screen.md ? 600 : 720}
  // ...
/>

<ModalForm
  width={screen.xs ? '100%' : screen.md ? 520 : 640}
  // ...
/>
```

### 5.7 表格响应式配置

表格在移动端的特殊处理：

```tsx
<ProTable
  scroll={{ x: screen.xs ? 800 : 'max-content' }}
  columns={columns.map(col => ({
    ...col,
    hideInTable: screen.xs && col.hideInMobile,
  }))}
  // 移动端隐藏部分列
/>
```

在列定义中控制显示：

```tsx
const columns: ProColumns<DataType>[] = [
  { title: 'ID', dataIndex: 'id', width: 80, hideInTable: true },
  { title: '名称', dataIndex: 'name', ellipsis: true },
  { title: '描述', dataIndex: 'description', ellipsis: true, hideInMobile: true },
  { title: '操作', valueType: 'option', fixed: 'right', width: 120 },
];
```

### 5.8 按钮响应式排列

工具栏按钮在移动端自动折行：

```tsx
import { Flex } from 'antd';

<Flex justify="space-between" wrap="wrap" gap={8}>
  <Input.Search placeholder="搜索..." style={{ minWidth: 200 }} />
  <Space wrap>
    <Button icon={<ExportOutlined />}>导出</Button>
    <Button type="primary" icon={<PlusOutlined />}>新建</Button>
  </Space>
</Flex>
```

### 5.9 响应式工具 Hook

封装响应式 Hook 简化开发：

```typescript
// hooks/useResponsive.ts
import { useToken } from '@ant-design/pro-components';

export const useResponsive = () => {
  const { screen } = useToken();
  const isMobile = screen.xs;
  const isTablet = screen.md && !screen.lg;
  const isDesktop = screen.lg;

  return { isMobile, isTablet, isDesktop, screen };
};
```

### 5.10 强制横屏提示

移动端页面可提示用户横屏以获得更好体验：

```tsx
import { useResponsive } from '@ant-design/pro-components';

const { screen } = useResponsive();

{screen.xs && (
  <Alert
    message="建议横屏查看"
    type="info"
    showIcon
    style={{ marginBottom: 16 }}
  />
)}
```

### 5.11 常见响应式布局模式

**列表页面**：
```tsx
<ProTable
  scroll={{ x: screen.xs ? 800 : 'max-content' }}
  // ...
/>
```

**详情页面**：
```tsx
<Row gutter={16}>
  <Col xs={24} md={12}><Card>左栏</Card></Col>
  <Col xs={24} md={12}><Card>右栏</Card></Col>
</Row>
```

**表单页面**：
```tsx
<Row gutter={16}>
  <Col xs={24} md={12}>
    <Form.Item label="字段1" name="field1">
      <Input />
    </Form.Item>
  </Col>
  <Col xs={24} md={12}>
    <Form.Item label="字段2" name="field2">
      <Input />
    </Form.Item>
  </Col>
</Row>
```

## 6. 接口交互与数据流

- 所有页面的数据流、接口调用、分页、权限、错误处理等，必须与后端 API 结构、权限标识、分页参数、ApiResponse 结构保持一致。
- 统一 message 提示，401/403/500 由 request 拦截器处理。

## 7. API 路由规范

前端调用 API 时必须与后端 Controller 路由完全匹配：

| 后端 Controller | 后端路由 | 前端调用示例 |
|----------------|----------|-------------|
| `/api/park/contracts` | POST /api/park/contracts/list | `{ method: 'POST', data: params }` |
| `/api/storage-quota` | GET /api/storage-quota/list | `{ params }` |
| `/api/park/contracts/payments` | POST /api/park/contracts/payments | `{ method: 'POST', data }` |

> **注意**：分页列表接口通常为 POST，使用 `{ data: params }` 而非 `{ params }`

---

## 样式规范速查

### 间距规范

| 元素 | 间距 |
|------|------|
| 区块内边距 | 16px |
| 分区间距 | 12px |
| 表单项上下 | 12px |
| 按钮组间距 | 8px |
| 列表顶部/底部留白 | 12px |

### 文字规范

| 元素 | 字号/字重 |
|------|------------|
| 一级标题 | 24px / 600 |
| 卡片标题 | 16px / 600 |
| 正文 | 14px / 400 |
| 次要文字 | 12px / 400 |

### 颜色规范

- 主色：使用 AntD token `colorPrimary`
- **禁止**使用自定义全局色板
- 状态色：统一使用 AntD 语义色 `success` / `info` / `warning` / `error`

### 组件规范

| 组件 | 规范 |
|------|------|
| 卡片 | `<Card bordered={false}>` |
| 表格 | ProTable + scroll={{ x: 'max-content' }} |
| 表单按钮 | 固定在底部吸底区或抽屉底部 |
| 删除按钮 | 必须使用 `Popconfirm` 确认 |
| 图标 | 使用 `@ant-design/icons`，尺寸 14/16px |

---

## 常见错误示例

### ❌ 错误 1：自定义全局样式

```tsx
// ❌ 禁止：自定义全局 CSS class
<div className="my-custom-class">

// ✅ 正确：使用 AntD 组件和内联样式
<div style={{ padding: 16 }}>
```

### ❌ 错误 2：ProTable 未添加列宽自适应

```tsx
// ❌ 禁止
<ProTable ... />

// ✅ 正确
<ProTable ... scroll={{ x: 'max-content' }} />
```

### ❌ 错误 3：搜索框未使用受控模式

```tsx
// ❌ 禁止
<Input.Search placeholder="搜索..." />

// ✅ 正确
<Input.Search
  placeholder="搜索..."
  value={state.search}
  onChange={(e) => set({ search: e.target.value })}
  onSearch={(value) => { set({ search: value }); actionRef.current?.reload(); }}
/>
```

### ❌ 错误 4：ProDescriptions 使用 items 属性

```tsx
// ❌ 禁止
<ProDescriptions items={ [...]} />

// ✅ 正确
<ProDescriptions column={2} bordered>
  <ProDescriptions.Item label="名称">{data.name}</ProDescriptions.Item>
</ProDescriptions>
```

### ❌ 错误 5：删除未使用 Popconfirm

```tsx
// ❌ 禁止
<Button danger onClick={handleDelete}>删除</Button>

// ✅ 正确
<Popconfirm title="确认删除？" onConfirm={handleDelete}>
  <Button danger>删除</Button>
</Popconfirm>
```

### ❌ 错误 6：自定义主色

```tsx
// ❌ 禁止：自定义主色
<div style={{ color: '#ff0000' }}>

// ✅ 正确：使用 AntD token
<div style={{ color: 'var(--ant-primary-color)' }}>
```

### ❌ 错误 7：非操作列设置固定

```tsx
// ❌ 禁止：非操作列设置 fixed
{ title: '时间', dataIndex: 'createdAt', width: 160, fixed: 'right' }

// ✅ 正确：仅操作列设置固定
{ title: '操作', key: 'action', width: 150, fixed: 'right' }
```

### ❌ 错误 8：分页接口使用 GET

```tsx
// ❌ 禁止：分页列表使用 GET
request('/api/list', { params: { page: 1, pageSize: 10 } })

// ✅ 正确：分页列表使用 POST + data
request('/api/list', { method: 'POST', data: { page: 1, pageSize: 10 } })
```
