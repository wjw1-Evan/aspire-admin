{
  "$schema": "https://opencode.ai/config.json",
  "lsp": {},
  "command": {
    "commit": {
      "template": "自动提交 Git（自动检测并执行）： ## 阶段 0：环境检查 1. 运行 git pull 拉取最新代码 2. 运行 git status 查看变更 3. 如果没有变更，终止并提示用户 4. 识别变更文件所属模块（前端/后端/配置文件） ## 阶段 1：代码检查 1. 前端文件变更 → 进入 Platform.Admin 运行 npm run lint 2. 后端文件变更 → 运行 dotnet build 3. 检查失败 → 修复后重新检查（最多 3 次），仍失败则终止 ## 阶段 2：智能 Commit 1. 运行 git diff --stat 分析变更 2. 按模块/目录拆分 commit（不同目录 = 不同 commit） 3. 生成符合约定的 commit message： - 类型前缀：feat/fix/docs/refactor/style/perf/test/chore - 使用简体中文 4. 执行 git add -A && git commit -m \"<message>\"\n\n## 阶段 3：推送\n1. 运行 git push 推送到远程\n2. 推送失败时等待 5 秒后重试一次\n\n## 规则\n- commit message 使用简体中文\n- 遵循约定式提交格式：type: message\n- lint/build 必须通过才能提交\n- 按模块拆分 commit，保持 atomic",
      "description": "自动提交 Git"
    }
  },
  "mcp": {
    "aspire": {
      "type": "local",
      "command": [
        "aspire",
        "mcp",
        "start"
      ],
      "enabled": true
    },
    "playwright": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@playwright/mcp@latest"
      ],
      "enabled": true
    },
    "context7": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@upstash/context7-mcp",
        "--api-key",
        "YOUR_API_KEY"
      ],
      "enabled": true
    },
    "filesystem": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@modelcontextprotocol/server-filesystem@latest",
        "/"
      ],
      "enabled": true
    }
  }
}
